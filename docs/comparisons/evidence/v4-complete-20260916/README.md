# V4.2 验收证据

日期：2026-09-16。说明与统计见 [综合验收报告](../../v4-completion.md)。

| 目录/文件 | 内容 |
| --- | --- |
| desktop | 最终 188/66 回归、跨进程/批处理汇总及 7 张原生截图 |
| viewer | Firestorm 实际窗口截图、观察记录及同时读取的原版权威状态 |
| web | 6 组浏览器截图和 JSON，含重载、文件、缓存及隔离头反例 |
| building | 真实建筑写入与搬移后读取报告、原生渲染 |
| gateway | 42 项正常、30 项故障、282 项 schema 检查，五个世代的请求/回执及事件 |
| storage | 44 项存储、12 项适配、6 项公开启动、3 项回滚检查，实际 SQLite 版本和产物摘要 |
| package-report.json | 全新含空格目录的离线包校验、安装及启动结果 |
| source-manifest.json | 本轮运行代码、工具、锁文件及测试输入 SHA256 |
| manifest.json | 本目录各证据文件 SHA256，不包含自身 |

JSON 在发布时统一为 UTF-8，并将开发工作目录前缀替换为 `<WORK>`；测试 UUID、trace 和相对路径保留。摘要指发布后的文件字节。截图为实际引擎或 Viewer 输出，未合成画面。

不发布服务器完整日志、账户密码、Bearer 令牌、会话密钥、数据库、编译缓存或用户存档。运行时故障检查的中间世代主要保存命令/回执，结论以顶层 recovery-report.json 为准；不以没有断言的中间空报告计数。首批原版 C01～C09 证据仍保存在上级历史目录。

源文件清单仅约束列出的运行输入，不表示编译结果可逐字节确定复现。桌面七张图分别为概览、编辑、地形、夜间、室内、组合与导入网格；UI 报告中的截图生成项与人工图像核对是两类证据。
